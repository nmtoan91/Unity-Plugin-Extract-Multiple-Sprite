using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace toanstt.MultipleSpritesSplit.Runtime
{
    public class MultipleSpritesSplitTestClass : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("MultipleSpritesSplitTestClass");
        }

        // Update is called once per frame
        void Update()
        {
            Debug.Log("MultipleSpritesSplitTestClass");
        }
    }
}
